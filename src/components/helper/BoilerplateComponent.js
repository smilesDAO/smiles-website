import { useRef, useEffect, useState } from "react";

const BoilerplateComponent = () => { // <= CHANGE THIS
  return (
    <div className="boilerplate-component-container"> {/* <= CHANGE THIS */}
    </div>
  );
};

export default BoilerplateComponent; // <= CHANGE THIS
